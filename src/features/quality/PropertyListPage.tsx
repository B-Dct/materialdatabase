import { PropertyRegistry } from './PropertyRegistry';

export function PropertyListPage() {
    return (
        <div className="p-8 max-w-[1600px] mx-auto pb-32 animate-in fade-in duration-500">
            <PropertyRegistry />
        </div>
    );
}
